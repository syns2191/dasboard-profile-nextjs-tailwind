"use client"
import React from 'react'

const AddtoCart = () => {
  return (
    <button className='btn bt' onClick={() => {
        console.log('click')
    }}>Add to Cart</button>
  )
}

export default AddtoCart